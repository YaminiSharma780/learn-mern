// Class component never uses HOOKS
import React, {Component} from "react";
export default class Contact extends Component{
    render(){
        return(
            <div>Contact</div>
        )
    }  
}